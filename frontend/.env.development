# Development Environment Variables for Frontend
# These are loaded in development mode

# Backend API base URL - empty to use Vite proxy
VITE_API_URL=

# WebSocket base URL - direct connection to backend (Vite proxy can be unreliable for WebSockets)
VITE_BACKEND_WS_URL=ws://localhost:8000

# Development mode flag (automatically set by Vite)
# VITE_MODE=development

# Auto-connect WebSockets in development
VITE_AUTO_CONNECT_WS=true
